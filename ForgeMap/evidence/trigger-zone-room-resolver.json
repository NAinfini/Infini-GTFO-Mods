{
  "family": "trigger-zone-room-resolver",
  "format": "gtfo-forge-native-evidence",
  "profile": "Forge-MapEditor-QA interop assemblies (read-only), decompiled with the machine's ilspycmd into %TEMP%\\fan\\tz-4\\ilspy",
  "question": "Which member returns a loaded prefab object for an authored `Assets/` resource path, and which member holds the prefab object a generated geomorph was created from?",
  "entry": {
    "assembly": "Shards-ASM.dll",
    "type": "AssetShards.AssetShardManager",
    "member": "GetLoadedAsset<T>(string assetPath, bool forceAssetDatabase = false)",
    "genericSignature": "public unsafe static T GetLoadedAsset<T>([DefaultParameterValue(null)] string assetPath, bool forceAssetDatabase = false) where T : UnityEngine.Object",
    "nonGenericSignature": "public unsafe static UnityEngine.Object GetLoadedAsset([DefaultParameterValue(null)] string assetPath, bool forceAssetDatabase = false)",
    "isStatic": true,
    "backingStore": {
      "member": "AssetShards.AssetShardManager.s_loadedAssetsLookup",
      "signature": "public unsafe static Dictionary<string, UnityEngine.Object> s_loadedAssetsLookup",
      "role": "the path-keyed table the loader fills; the path an authored reference carries is the key this lookup is asked with"
    },
    "loadStateMembers": [
      "public unsafe static bool StartupAssetsIsLoaded",
      "public unsafe static bool SharedAssetsIsLoaded",
      "public unsafe static bool EnemyAssetsIsLoaded"
    ],
    "existingProductionUse": "ForgeEnemy/Native/EnemySpawnRequirementSource.cs:174 calls AssetShardManager.GetLoadedAsset<GameObject>(path, false) with the base-prefab paths of enabled EnemyDataBlocks; the same entry is used here with an authored room's source prefab path."
  },
  "identity": {
    "assembly": "Modules-ASM.dll",
    "type": "LevelGeneration.LG_Geomorph",
    "members": [
      { "name": "m_geoPrefab", "signature": "public unsafe UnityEngine.GameObject LG_Geomorph::get_m_geoPrefab()", "kind": "field", "role": "the prefab object this geomorph was created from; compared by object reference against the object GetLoadedAsset returned for the authored path" },
      { "name": "m_zone", "signature": "public unsafe LevelGeneration.LG_Zone LG_Geomorph::get_m_zone()", "kind": "field", "role": "the zone the geomorph stands in" },
      { "name": "m_areas", "signature": "Il2CppInterop.Runtime.InteropTypes.Arrays.Il2CppReferenceArray`1<LevelGeneration.LG_Area> LG_Geomorph::get_m_areas()", "kind": "field", "role": "the geomorph's own areas" }
    ],
    "traversal": [
      { "assembly": "Modules-ASM.dll", "type": "LevelGeneration.LG_Zone", "member": "m_areas", "signature": "public unsafe Il2CppSystem.Collections.Generic.List`1<LevelGeneration.LG_Area> LG_Zone::get_m_areas()", "role": "the zone's own area list: the level's own statement of which rooms it built in this zone" },
      { "assembly": "Modules-ASM.dll", "type": "LevelGeneration.LG_Area", "member": "m_geomorph", "signature": "public unsafe LevelGeneration.LG_Geomorph LG_Area::get_m_geomorph()", "role": "the geomorph an area belongs to" },
      { "assembly": "Modules-ASM.dll", "type": "LevelGeneration.LG_Zone", "member": "m_dimensionIndex", "signature": "public unsafe GameData.eDimensionIndex LG_Zone::get_m_dimensionIndex()", "role": "the zone's dimension coordinate" },
      { "assembly": "Modules-ASM.dll", "type": "LevelGeneration.LG_Zone", "member": "m_layer", "signature": "public unsafe LevelGeneration.LG_Layer LG_Zone::get_m_layer()", "role": "the zone's layer, read as m_layer.m_type" },
      { "assembly": "Modules-ASM.dll", "type": "LevelGeneration.LG_Zone", "member": "LocalIndex", "signature": "GameData.eLocalZoneIndex LevelGeneration.LG_Zone::get_LocalIndex()", "role": "the zone's index inside its layer" }
    ],
    "existingProductionReaders": [
      "ForgeMap/Native/ZoneIndex.cs: Build reads LG_LevelBuilder.Current.m_currentFloor.allZones; Coordinates(LG_Zone) reads m_dimensionIndex / m_layer.m_type / LocalIndex; Zones() enumerates the floor's own zone order; TryCoordinates resolves a zone to those coordinates.",
      "ForgeDevelopment/Native/WorldInspection.cs:88-128 traverses zone.m_areas -> area.m_geomorph, de-duplicated by geomorph.GetInstanceID(); the resolver uses the same traversal rather than a second walk."
    ]
  },
  "comparison": "AuthoringPrefabPath --AssetShardManager.GetLoadedAsset<GameObject>(path, false)--> UnityEngine.GameObject (asset object) --Pointer--> System.IntPtr, compared against LG_Geomorph.m_geoPrefab --Pointer--> System.IntPtr. Object reference equality is the identity; `m_geoPrefab.name` is never compared, because a prefab's display name is not the original Assets/ identity.",
  "unproven": [
    "That LG_Geomorph.m_geoPrefab holds the loaded prefab asset object rather than a copy of it. The interop stub proves the field and its GameObject type, not which object the builder stores; if the builder stores the instantiated root, the reference comparison never matches and every authored room is refused by name. In-game check: print, for one generated geomorph, whether its m_geoPrefab pointer equals the pointer AssetShardManager.GetLoadedAsset<GameObject>(<its authored path>, false) returns.",
    "That the level's geomorph prefabs are still present in s_loadedAssetsLookup while the level runs (an unloaded shard would make the lookup answer null). In-game check: read the authored path's loaded asset at the same moment a trigger zone is placed.",
    "That every geomorph of a zone is reachable through that zone's own m_areas list (a geomorph with no area is invisible to this walk). In-game check: count zone.m_areas-derived geomorphs against the geomorphs the development inspection already traverses on a real level."
  ],
  "verificationPoints": [
    "A zone document whose room this level holds exactly once must be placed at the room's world pose, and its entered/exited facts must fire at the composed world volume.",
    "A room prefab used in two zones of the same level must be refused by name for the zone referencing it (the reference carries an author zone id, which this assembly cannot map to a native zone; the answer therefore has to be unique in the level).",
    "A room reference whose prefab path loads nothing must be refused by name, with one report line, and it must never be judged at the document pose."
  ]
}
